## 看到的注解

```
@ConditionalOnExpression("!'${spring.application.name}'.equals('paascloud-provider-product')") // 当项目不是paascloud-provider-product，加载这个bean


@Autowired


 @GrpcClient("paascloud-provider-product")
 
 @GrpcService
 
 @ConfigurationProperties(EurekaClientConfigBean.PREFIX)
 
@Import({MapperScannerRegistrar.class})
public @interface MapperScan {
 
```

